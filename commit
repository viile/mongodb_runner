#!/usr/bin/env python3
"""Short launcher for the standalone git commit helper."""

from __future__ import annotations

import runpy
from pathlib import Path


if __name__ == "__main__":
    tool_path = Path(__file__).resolve().parent / "tools" / "git_commit.py"
    runpy.run_path(str(tool_path), run_name="__main__")
